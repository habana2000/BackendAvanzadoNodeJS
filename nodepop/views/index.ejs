<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <h2>Lista de anuncios</h2>
    <table>

      <% anuncios.forEach(anuncio => { %>
        <tr>
          <td>  
            <p>Anuncio: <%= anuncio.nombre %> --> Precio: <%= anuncio.precio %> €</p>
            <p>Venta? <%= anuncio.venta %> // Tags: <%= anuncio.tags %></p>
          </td>    
          <td>
            <img src="../images/anuncios/<%= anuncio.foto %>" alt="<%= anuncio.foto %>" width=200>
          </td>
        </tr>
        <% }) %>
        
    </table>
  </body>
</html>
